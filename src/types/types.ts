export type chartProps = {
    title:string
    axis?:boolean
}
export type ChartDataValue = {
    label: string;
    income: number;
    expense: number;
}
export type DummyCountryValue = {
    label: string;
    value: number;
}

export type CategoryValues = {
    label: string;
    value: number;
}